# Contributing

Run `python -m ruff check .`, `python -m compileall -q app`, and
`python -m pytest -q` before opening a pull request. Every grader or metric
change must include a regression test and explain how release decisions change.
Use synthetic evaluation cases only.
